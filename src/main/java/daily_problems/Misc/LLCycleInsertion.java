package daily_problems.Misc;

public class LLCycleInsertion {

//    public LLNode insert(LLNode node, int i) {
//
//        if (node.next == node) { //there is only one item - just need to add new node
//            LLNode newNode = new LLNode(i, null);
//            newNode.setNext(node);
//            node.setNext(newNode);
//            return newNode;
//        }
//
//        LLNode curr = node;
//        LLNode next = node.next;
//
//        while () {
//            if (curr.data > i && i < next.data) {   //node needs to be put between two in order nodes
//                LLNode newNode = new LLNode(i, null);
//                LLNode temp = next;
//                curr.next = newNode;
//                newNode.next = temp;
//                return newNode;
//            } else if (curr.data < i && i > next.data) { //its at the end of the cycle
//
//            } else if (curr.data < i && i < next.data) { //it is at the beginning of the cycle
//
//            }
//            curr = next;
//            next = next.next;
//        }
//    }
}
